# Full research environment for the install flows documented in README.md.
# pyproject.toml is the source of truth for version constraints; this file
# records one known-good resolution of the runtime dependencies plus every
# optional extra (gpu, tracking, tuning, carbon, research).

# Core ML/DL framework (resolves torch>=2.0 / torchvision>=0.15)
torch==2.4.0
torchvision==0.19.0

# Runtime dependencies
numpy>=1.21
PyYAML>=6.0
tqdm>=4.0          # progress bars in the FF/MF/CaFo training loops

# gpu extra: NVML energy/memory monitoring
pynvml==12.0.0

# tracking extra: Weights & Biases experiment tracking
wandb==0.19.8

# carbon extra: CodeCarbon CO2e estimation
codecarbon==3.0.1

# tuning extra: Optuna hyperparameter search
optuna==4.2.1

# research extra: environment loading and analysis utilities
python-dotenv
scikit-learn
